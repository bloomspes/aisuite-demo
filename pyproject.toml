[project]
name = "aisuite-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn",
  "sse-starlette",
  "python-dotenv",
  "openai",
  "anthropic",
  "redis",
  "aisuite",
  "click>=8.2.1",
]
